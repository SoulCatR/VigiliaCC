# Environment
NODE_ENV=development

# Server
PORT=3000
API_NAME=VigiliaCC-API

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vi_vigilia_cc_dev
DB_USER=edwin_tejada
DB_PASSWORD=C&VbwuHB8q

# JWT Secret (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=cambiar_por_secret_seguro_de_64_caracteres_hexadecimales

# CORS (separar con comas)
ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100