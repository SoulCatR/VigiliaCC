NODE_ENV=development
PORT=3000

# PostgreSQL Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=vi_vigilia_cc_dev
DB_USER=postgres
DB_PASSWORD=tu_password_seguro

# JWT Secret (generar uno único: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))")
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Configuración de CORS (separar con comas)
ALLOWED_ORIGINS=http://localhost:3001,http://localhost:5173